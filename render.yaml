services:
  - type: web
    name: omok-link
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /r/health
    envVars:
      - key: NODE_VERSION
        value: "20"
